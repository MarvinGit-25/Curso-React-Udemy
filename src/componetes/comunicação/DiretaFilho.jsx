const Filho = props => {
    return (
        <div>
            <span>{props.nome} </span>
            <span>{props.idade} </span>
            
            
        </div>
    )
}

export default Filho